#' forecastsuite: Multi-Model Time Series Forecasting Toolkit and Local App
#'
#' Extends the Forecast Dashboard's Prophet/ARIMA workflow with a pluggable
#' model registry (ARIMA, SARIMA, ETS, TBATS, NNETAR, Holt-Winters, and
#' optional LSTM via torch) and a bundled local Shiny app exposing all
#' models without the hosted app's memory-conserving constraints. See
#' `vignette("forecastsuite")` for a full walkthrough, or call
#' [run_app()] to launch the bundled app directly.
#'
#' @keywords internal
"_PACKAGE"
