% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{impute_anomalies}
\alias{impute_anomalies}
\title{Impute detected anomalies before fitting}
\usage{
impute_anomalies(
  df,
  date_agg = "day",
  method = c("iqr", "zscore"),
  threshold = 1.5
)
}
\arguments{
\item{df}{A tibble with \code{ds}/\code{y} columns.}

\item{date_agg}{Aggregation frequency: one of \code{"hour"}, \code{"day"},
\code{"week"}, \code{"month"}, \code{"quarter"}, \code{"year"}.}

\item{method}{\code{"iqr"} (the default) or \code{"zscore"} -- passed through to
\code{\link[=detect_anomalies]{detect_anomalies()}}.}

\item{threshold}{Numeric sensitivity threshold, passed through to
\code{\link[=detect_anomalies]{detect_anomalies()}}. Default \code{1.5}.}
}
\value{
A tibble with columns \code{ds}, \code{y} (the cleaned series). The count
of points that were flagged and replaced is attached as
\code{attr(result, "n_imputed")}.
}
\description{
Runs \code{\link[=detect_anomalies]{detect_anomalies()}}, replaces flagged points with \code{NA}, and
re-interpolates with the same fill idiom used throughout this package
(\code{zoo::na.approx()}, remaining edge \code{NA}s filled with the series mean) --
a cleaned series to fit on instead of the raw one, so a handful of
spikes/dips don't distort the fitted model the way they would
untouched. Distinct from \code{\link[=decompose_series]{decompose_series()}}'s \code{robust} argument, which
downweights outliers during decomposition rather than removing them
before fitting; the two can be used independently or together.
}
\examples{
df <- tibble::tibble(ds = as.Date("2024-01-01") + 0:29, y = c(rep(10, 29), 100))
impute_anomalies(df, date_agg = "day", method = "iqr")
}
