% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contingency.R
\name{holiday_contingency}
\alias{holiday_contingency}
\title{Run both holiday consistency checks at once}
\usage{
holiday_contingency(holidays, data)
}
\arguments{
\item{holidays}{A tibble with a \code{ds} column, the compiled holiday list.}

\item{data}{A tibble with \code{ds}/\code{y} columns, the finalized dataset.}
}
\value{
A list with \code{with_data} (see \code{\link[=holidays_with_data]{holidays_with_data()}}) and
\code{always_zero} (see \code{\link[=always_zero_dates]{always_zero_dates()}}).
}
\description{
Bundles \code{\link[=holidays_with_data]{holidays_with_data()}} and \code{\link[=always_zero_dates]{always_zero_dates()}} -- the app's
full "consistency check."
}
\examples{
holidays <- tibble::tibble(ds = as.Date("2024-01-01"), holiday = "New Year")
data <- tibble::tibble(ds = as.Date("2024-01-01"), y = 5)
holiday_contingency(holidays, data)
}
