% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{list_models}
\alias{list_models}
\title{List every registered model}
\usage{
list_models(available_only = TRUE)
}
\arguments{
\item{available_only}{Logical; if \code{TRUE} (the default), omit any model
whose \code{requires} package (see \code{\link[=register_model]{register_model()}}) isn't installed --
e.g. \code{"lstm"} is omitted until \code{torch} is available.}
}
\value{
A list of registered model entries (see \code{\link[=get_model]{get_model()}} for the
shape of each one).
}
\description{
List every registered model
}
\examples{
vapply(list_models(available_only = TRUE), function(m) m$key, character(1))
}
