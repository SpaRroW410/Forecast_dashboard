% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{compute_interval_coverage}
\alias{compute_interval_coverage}
\title{Empirical prediction-interval coverage}
\usage{
compute_interval_coverage(fc_tib, test_df, nominal_level = 0.8)
}
\arguments{
\item{fc_tib}{A tibble with \code{ds}/\code{yhat} columns, and typically
\code{yhat_lower}/\code{yhat_upper}. Models with no interval columns (e.g.
NNETAR without \code{PI = TRUE}) return \code{NA} coverage rather than erroring.}

\item{test_df}{A tibble with \code{ds}/\code{y} columns -- the held-out actuals.}

\item{nominal_level}{Numeric, the interval's nominal confidence level.
Default \code{0.8}.}
}
\value{
A list with \code{empirical_coverage}, \code{nominal_level}, \code{gap}
(\code{empirical_coverage - nominal_level}), and \code{n}.
}
\description{
Whether a model's uncertainty bounds are honest, not just its point
forecast: what fraction of actuals actually fell inside
\code{yhat_lower}/\code{yhat_upper}, versus the interval's nominal confidence
level (Prophet and the \code{forecast}-package adapters default to an 80\%
interval).
}
\examples{
ds <- as.Date("2024-01-01") + 0:9
fc_tib <- tibble::tibble(ds = ds, yhat = 10, yhat_lower = 9, yhat_upper = 11)
test_df <- tibble::tibble(ds = ds, y = c(rep(10, 8), 20, 20))
compute_interval_coverage(fc_tib, test_df)
}
