% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{decompose_series}
\alias{decompose_series}
\title{Seasonal decomposition of a series}
\usage{
decompose_series(df, date_agg = "day", robust = FALSE)
}
\arguments{
\item{df}{A tibble with \code{ds}/\code{y} columns.}

\item{date_agg}{Aggregation frequency: one of \code{"hour"}, \code{"day"},
\code{"week"}, \code{"month"}, \code{"quarter"}, \code{"year"}.}

\item{robust}{Logical; use robust (outlier-downweighted) STL --
\code{stats::stl(..., robust = TRUE)} -- instead of ordinary STL, so a
handful of spikes/dips don't skew the trend/seasonal split. Default
\code{FALSE}.}
}
\value{
A tibble with columns \code{ds}, \code{observed}, \code{trend}, \code{seasonal},
\code{remainder}, or \code{NULL} (not an error) when no candidate frequency has
enough cycles of data, or \code{date_agg} has no seasonal candidates at all
(e.g. \code{"year"}).
}
\description{
Surfaces the STL trend/seasonal/remainder split that \code{analyze_series()}
already computes internally to drive the recommendation heuristic, as a
tidy tibble a caller can plot directly.
}
\examples{
n <- 60
df <- tibble::tibble(ds = as.Date("2024-01-01") + 0:(n - 1),
                      y = 100 + 10 * sin(2 * pi * seq_len(n) / 7) + rnorm(n))
decompose_series(df, date_agg = "day")
}
