% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest_params.R
\name{suggest_parameters}
\alias{suggest_parameters}
\title{Suggest starting hyperparameters for one model}
\usage{
suggest_parameters(analysis, model_key)
}
\arguments{
\item{analysis}{The list returned by \code{\link[=analyze_series]{analyze_series()}}.}

\item{model_key}{A registry key, e.g. \code{"prophet"}, \code{"arima"},
\code{"sarima"}. Models with no tunable parameters return an empty
suggestion.}
}
\value{
A list with \code{params} (a named list of suggested argument
values, matching that model's \code{fit()} parameters), \code{text} (a one-line
human-readable summary), and \code{reasons} (a character vector explaining
each suggestion).
}
\description{
Derives starting-point hyperparameters from \code{\link[=analyze_series]{analyze_series()}}'s
diagnostics rather than fitting a grid of candidates. These are informed
starting points, not optimal values -- for ARIMA/SARIMA, \code{auto.arima()}
still searches the order space better than this heuristic; the
suggested \code{p}/\code{q} matter mainly for hand-tuning, while \code{d}/\code{D} come
straight from \code{ndiffs()}/\code{nsdiffs()}.
}
\examples{
df <- tibble::tibble(ds = as.Date("2024-01-01") + 0:59, y = 1:60 + rnorm(60))
suggest_parameters(analyze_series(df, "day"), "arima")
}
