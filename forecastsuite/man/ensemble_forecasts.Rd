% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ensemble.R
\name{ensemble_forecasts}
\alias{ensemble_forecasts}
\title{Combine several models' forecasts into one}
\usage{
ensemble_forecasts(
  forecasts,
  method = c("mean", "inverse_error"),
  errors = NULL
)
}
\arguments{
\item{forecasts}{A named list of \code{ds}/\code{yhat} tibbles (names are the model
labels).}

\item{method}{\code{"mean"} (the default) weights every model equally;
\code{"inverse_error"} weights each model by \code{1/errors[[name]]}. A model
missing from \code{errors}, or with a non-positive/\code{NA} error, falls back to
equal weight rather than dividing by zero.}

\item{errors}{Required for \code{method = "inverse_error"}: a named list or
vector of each model's own test error (e.g. RMSE or MASE, from
\code{\link[=safe_compute_metrics]{safe_compute_metrics()}} -- not re-derived here), keyed by the same
names as \code{forecasts}.}
}
\value{
A tibble with columns \code{ds} and \code{yhat} -- the combined forecast.
}
\description{
Averages (simple or inverse-error-weighted) several already-fit models'
forecasts -- no re-fitting. Models that don't share every \code{ds} are
handled by renormalizing weights per-row over whichever models actually
have a value there.
}
\examples{
ds <- as.Date("2024-01-01") + 0:2
forecasts <- list(
  A = tibble::tibble(ds = ds, yhat = c(10, 10, 10)),
  B = tibble::tibble(ds = ds, yhat = c(20, 20, 20))
)
ensemble_forecasts(forecasts, method = "mean")
ensemble_forecasts(forecasts, method = "inverse_error", errors = list(A = 1, B = 5))
}
