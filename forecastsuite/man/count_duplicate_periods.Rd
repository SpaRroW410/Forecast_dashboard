% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_period.R
\name{count_duplicate_periods}
\alias{count_duplicate_periods}
\title{Count how many rows share a period}
\usage{
count_duplicate_periods(df, date_agg = "day", group_col = NULL)
}
\arguments{
\item{df}{A data frame with a \code{ds} (date-like) column.}

\item{date_agg}{Aggregation frequency: one of \code{"hour"}, \code{"day"},
\code{"week"}, \code{"month"}, \code{"quarter"}, \code{"year"}.}

\item{group_col}{Optional character scalar, a grouping column.}
}
\value{
A single integer: how many rows would be removed by collapsing
(\code{0} if none).
}
\description{
Used to tell the user what \code{\link[=collapse_to_period]{collapse_to_period()}} would do, before doing
it. With \code{group_col} supplied, rows only count as duplicates if they
share both a period and a group -- two different districts on the same
date are not a collision.
}
\examples{
df <- data.frame(ds = as.Date(c("2024-01-01", "2024-01-02")), y = c(3, 5))
count_duplicate_periods(df, date_agg = "month")
}
