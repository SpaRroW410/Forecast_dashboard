% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{group_correlation_matrix}
\alias{group_correlation_matrix}
\title{Pairwise correlation between grouped series}
\usage{
group_correlation_matrix(grouped_series_list)
}
\arguments{
\item{grouped_series_list}{A named list of \code{ds}/\code{y} tibbles, one per
group (e.g. the app's \code{grouped_series()}).}
}
\value{
A tibble with columns \code{group_a}, \code{group_b}, \code{correlation} --
upper triangle only, no self-pairs, one row per distinct pair. Empty
if fewer than 2 groups are supplied.
}
\description{
Correlation between every pair of configured groups' series, aligned on
\code{ds} (pairwise-complete, so groups that don't share every period still
compare on their overlap).
}
\examples{
ds <- as.Date("2024-01-01") + 0:9
groups <- list(
  A = tibble::tibble(ds = ds, y = 1:10),
  B = tibble::tibble(ds = ds, y = 10:1)
)
group_correlation_matrix(groups)
}
