% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aggregate_period.R
\name{collapse_to_period}
\alias{collapse_to_period}
\title{Collapse multiple rows sharing a period into one}
\usage{
collapse_to_period(
  df,
  date_agg = "day",
  fun = c("sum", "mean", "median", "none"),
  group_col = NULL
)
}
\arguments{
\item{df}{A data frame with \code{ds} (date-like) and \code{y} (numeric) columns.}

\item{date_agg}{Aggregation frequency: one of \code{"hour"}, \code{"day"},
\code{"week"}, \code{"month"}, \code{"quarter"}, \code{"year"}.}

\item{fun}{How to combine rows sharing a period: \code{"sum"} (the default,
for counts), \code{"mean"}/\code{"median"} (for rates/indices, where summing
across strata would be meaningless), or \code{"none"} (return \code{df}
unchanged).}

\item{group_col}{Optional character scalar; rows only collapse together
if they share both a period and this column's value.}
}
\value{
A tibble with one row per period (and group, if \code{group_col} is
supplied), columns \code{ds}/\code{y}(/\code{group_col}).
}
\description{
Snaps each row's \code{ds} to the start of its period (per \code{date_agg}) and
combines rows that land on the same period (and, if \code{group_col} is
supplied, the same group) -- e.g. case data split by district/sex/age
with many rows per Year+Quarter.
}
\examples{
df <- data.frame(ds = as.Date(c("2024-01-01", "2024-01-02")), y = c(3, 5))
collapse_to_period(df, date_agg = "month")
}
