% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouping_utils.R
\name{split_by_group}
\alias{split_by_group}
\title{Split a ds/y/group tibble into one tibble per group}
\usage{
split_by_group(df, group_col)
}
\arguments{
\item{df}{A data frame with \code{ds}, \code{y}, and \code{group_col} columns.}

\item{group_col}{Character scalar, the grouping column's name, or \code{NULL}
(returns \code{NULL} -- no grouping).}
}
\value{
A named list of \code{ds}/\code{y} tibbles, one per distinct value of
\code{group_col} (names sorted), or \code{NULL} if \code{group_col} is \code{NULL} or not
present in \code{df}.
}
\description{
Split a ds/y/group tibble into one tibble per group
}
\examples{
df <- data.frame(ds = as.Date("2024-01-01") + 0:3,
                  y = 1:4, District = c("A", "A", "B", "B"))
split_by_group(df, "District")
}
