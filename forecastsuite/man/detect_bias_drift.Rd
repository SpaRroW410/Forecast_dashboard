% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias_drift.R
\name{detect_bias_drift}
\alias{detect_bias_drift}
\title{Check whether forecast bias is growing across the test window}
\usage{
detect_bias_drift(fc_tib, test_df, n_splits = 2)
}
\arguments{
\item{fc_tib}{A tibble with \code{ds}/\code{yhat} columns -- a model's forecast.}

\item{test_df}{A tibble with \code{ds}/\code{y} columns -- the held-out actuals.}

\item{n_splits}{Integer, how many sequential chunks to split the window
into. Default \code{2}. Capped so every chunk gets at least one row rather
than erroring on a short window.}
}
\value{
A list with \code{chunks} (a tibble of \code{chunk}/\code{bias}/\code{pct_bias}/\code{n},
one row per chunk) and \code{drifting} (\code{TRUE} if \verb{|bias|} strictly
increases chunk over chunk, \code{FALSE} if not, \code{NA} if there isn't enough
data for a read).
}
\description{
Splits the test window into \code{n_splits} sequential, roughly-equal chunks
(oldest first) and computes \code{\link[=compute_forecast_bias]{compute_forecast_bias()}} in each -- a
simple, explainable drift signal (is \verb{|bias|} growing chunk over chunk),
not a full statistical trend test.
}
\examples{
ds <- as.Date("2024-01-01") + 0:9
fc_tib <- tibble::tibble(ds = ds, yhat = c(rep(10, 5), rep(6, 5)))
test_df <- tibble::tibble(ds = ds, y = rep(10, 10))
detect_bias_drift(fc_tib, test_df, n_splits = 2)
}
