% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{analyze_series}
\alias{analyze_series}
\title{Analyze a series' shape for model recommendation}
\usage{
analyze_series(df, date_agg = "day", robust = FALSE)
}
\arguments{
\item{df}{A tibble with \code{ds}/\code{y} columns.}

\item{date_agg}{Aggregation frequency: one of \code{"hour"}, \code{"day"},
\code{"week"}, \code{"month"}, \code{"quarter"}, \code{"year"}.}

\item{robust}{Logical; use robust (outlier-downweighted) STL when a
seasonal decomposition is attempted internally. Default \code{FALSE}.}
}
\value{
A list with \code{n_obs}, \code{detected_freq}, \code{n_cycles},
\code{trend_strength}, \code{seasonal_strength}, \code{ndiffs_needed},
\code{nsdiffs_needed}, \code{missing_ratio}, \code{arima_p}, \code{arima_q}, \code{arima_P},
\code{arima_Q}.
}
\description{
A lightweight, no-model-fitting heuristic: measures trend/seasonal
strength (via STL decomposition), differencing needed
(\code{forecast::ndiffs}/\code{nsdiffs}), missing-data ratio, and a Box-Jenkins
ACF/PACF-based (p,q)(P,Q) order suggestion. Feeds \code{\link[=recommend_model]{recommend_model()}}'s
scoring and the app's suggested-hyperparameters table.
}
\examples{
df <- tibble::tibble(ds = as.Date("2024-01-01") + 0:29, y = 1:30 + rnorm(30))
analyze_series(df, date_agg = "day")
}
