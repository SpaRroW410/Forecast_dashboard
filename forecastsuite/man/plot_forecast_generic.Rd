% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_generic.R
\name{plot_forecast_generic}
\alias{plot_forecast_generic}
\title{Plot a single model's forecast}
\usage{
plot_forecast_generic(
  forecast_df,
  train_df = NULL,
  model_obj = NULL,
  subtitle = NULL,
  title = "Forecast",
  show_trend = TRUE,
  show_uncertainty = TRUE,
  show_holidays = FALSE,
  show_changepoints = FALSE,
  max_marker_lines = 60,
  color_actual = "#1b9e77",
  color_forecast = "#d95f02",
  color_trend = "#7570b3",
  color_ci = "#1b9e77"
)
}
\arguments{
\item{forecast_df}{A tibble with \code{ds}/\code{yhat} (and optionally
\code{yhat_lower}/\code{yhat_upper}/\code{trend}/\code{holidays}) columns.}

\item{train_df}{Optional tibble with \code{ds}/\code{y} columns, drawn as the
"Actual" line.}

\item{model_obj}{Optional fitted model object; only used to read
\verb{$changepoints} when \code{show_changepoints = TRUE}.}

\item{subtitle}{Optional character scalar, e.g. an ARIMA/SARIMA
\code{annotate()} result like \code{"ARIMA(2,1,1)(1,0,0)[12]"}.}

\item{title}{Character scalar, the plot title. Default \code{"Forecast"}.}

\item{show_trend, show_uncertainty, show_holidays, show_changepoints}{Logical toggles for each optional layer.}

\item{max_marker_lines}{Integer cap on how many holiday/changepoint
marker lines are drawn (evenly subsampled beyond this), so a long
horizon with recurring holidays doesn't render as unreadable clutter.}

\item{color_actual, color_forecast, color_trend, color_ci}{Hex color
strings for each line/ribbon.}
}
\value{
A \code{plotly} htmlwidget.
}
\description{
Model-agnostic plotly plotting: works off just \code{ds}/\code{yhat}(+intervals)
for models that only produce that, and additionally draws Prophet's
trend/holiday/changepoint decomposition when \code{forecast_df} still has
those columns (pass the adapter's raw \code{forecast()} output for that; pass
the standardized \code{to_tibble()} output to metrics functions instead).
}
\examples{
ds <- as.Date("2024-01-01") + 0:9
forecast_df <- tibble::tibble(ds = ds, yhat = 1:10)
plot_forecast_generic(forecast_df)
}
