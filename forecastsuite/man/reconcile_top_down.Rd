% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reconciliation.R
\name{reconcile_top_down}
\alias{reconcile_top_down}
\title{Top-down hierarchical reconciliation (historical proportions)}
\usage{
reconcile_top_down(fits, aggregate_forecast)
}
\arguments{
\item{fits}{A named list of fit-result lists (one per group), each with
\verb{$train} (a tibble with \code{ds}/\code{y}) used only to compute historical
proportions -- \verb{$fc_tib} is not used here (unlike
\code{\link[=reconcile_bottom_up]{reconcile_bottom_up()}}, which sums it). A \code{NULL} entry is dropped. A
group whose historical total is zero/NA falls back to an equal share
rather than a zero share.}

\item{aggregate_forecast}{A tibble with \code{ds}/\code{yhat} columns (and
optionally \code{yhat_lower}/\code{yhat_upper}) -- a forecast fit directly on
the summed series, not derived from \code{fits}.}
}
\value{
A list with \code{fc_tib} (\code{aggregate_forecast}'s own columns,
unchanged -- the top-down total \emph{is} the aggregate-level forecast),
\code{train}/\code{test} (\code{NULL}; the caller already has the aggregate-level
train/test that produced \code{aggregate_forecast} and should use those
directly for display), \code{components} (the names of the groups whose
historical shares were used), and \code{proportions} (a named list, one
fixed share per group, summing to 1).
}
\description{
Disaggregates one already-fit aggregate-level forecast down to each
group by that group's fixed historical share of the total, instead of
fitting (or summing) each group's own forecast shape the way
\code{\link[=reconcile_bottom_up]{reconcile_bottom_up()}} does. Coherent by construction (the shares sum
to 1), and a meaningfully different result from bottom-up whenever the
aggregate-level model captures dynamics the per-group models miss (or
vice versa) -- not just the same total recomputed a different way.
}
\examples{
ds <- as.Date("2024-01-01") + 0:2
fits <- list(
  A = list(train = tibble::tibble(ds = ds, y = c(9, 10, 11))),
  B = list(train = tibble::tibble(ds = ds, y = c(1, 1, 1)))
)
aggregate_forecast <- tibble::tibble(ds = ds, yhat = c(10, 11, 12))
reconcile_top_down(fits, aggregate_forecast)
}
