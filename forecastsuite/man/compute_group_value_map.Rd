% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grouping_utils.R
\name{compute_group_value_map}
\alias{compute_group_value_map}
\title{Build a raw-value -> canonical-label map for a grouping column}
\usage{
compute_group_value_map(raw_values, merge_case = TRUE)
}
\arguments{
\item{raw_values}{The full grouping column, with repeats -- frequency
matters, since the most common casing/spacing variant becomes each
cluster's canonical label (ties broken by whichever appears first).}

\item{merge_case}{Logical; if \code{TRUE} (the default), values identical
after \code{trimws()}+\code{tolower()} collapse into one label; if \code{FALSE},
every distinct raw value keeps its own label.}
}
\value{
A tibble with columns \code{raw} (every distinct input value) and
\code{label} (its canonical label).
}
\description{
Real-world grouping columns are often messy: \code{"female"}/\code{"Female"}/
\code{"FEMALE"} in the same column, meant to be one group, not three. Builds a
raw-value -> canonical-label mapping (one row per distinct raw value)
that the Import tab's "Merge / relabel values" table starts from and the
user can further edit by hand.
}
\examples{
compute_group_value_map(c("female", "Female", "male"))
}
