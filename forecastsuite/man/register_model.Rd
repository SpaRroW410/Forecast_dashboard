% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_model}
\alias{register_model}
\title{Register a forecasting model}
\usage{
register_model(
  key,
  label,
  fit,
  forecast,
  to_tibble,
  supports_holidays = FALSE,
  requires = NULL,
  annotate = NULL
)
}
\arguments{
\item{key}{Character scalar, a unique registry key (e.g. \code{"arima"}).}

\item{label}{Character scalar, the human-readable name shown in the UI.}

\item{fit}{Function with signature \code{fit(train_df, date_agg, ...)}
returning a fitted model object.}

\item{forecast}{Function with signature \code{forecast(model_obj, h, ...)}
returning the model's raw forecast object.}

\item{to_tibble}{Function with signature
\code{to_tibble(raw_forecast_obj, test_df)} returning a tibble with columns
\code{ds}, \code{yhat}, and optionally \code{yhat_lower}/\code{yhat_upper}.}

\item{supports_holidays}{Logical; whether \code{fit()} accepts a
\code{holidays_df} argument and models holiday effects. Default \code{FALSE}.}

\item{requires}{Character scalar naming an optional package this model
needs (e.g. \code{"torch"}), or \code{NULL} if it has no extra dependency.
\code{list_models(available_only = TRUE)} omits the model until that
package is installed.}

\item{annotate}{Optional function with signature \code{annotate(model_obj)}
returning a one-line character description of the fitted model (e.g.
\code{"ARIMA(2,1,1)(1,0,0)[12]"}), shown as a plot subtitle.}
}
\value{
\code{TRUE}, invisibly.
}
\description{
Adds a model to the package's pluggable registry, keyed by \code{key}. Every
model implements the same three-function contract (\code{fit}/\code{forecast}/
\code{to_tibble}), so once registered it works everywhere in the bundled app
(Model tab, Compare Selected Models, Cross-Validation, the Backtest
Leaderboard, the Show Code panel) with no other code changes.
}
\examples{
register_model(
  key = "naive_last",
  label = "Naive (last value)",
  fit = function(train_df, date_agg = "day", ...) utils::tail(train_df$y, 1),
  forecast = function(model_obj, h, ...) list(mean = rep(model_obj, h)),
  to_tibble = function(fc_obj, test_df) {
    tibble::tibble(ds = test_df$ds[seq_along(fc_obj$mean)], yhat = fc_obj$mean)
  }
)
}
