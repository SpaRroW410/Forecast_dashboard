% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{detect_anomalies}
\alias{detect_anomalies}
\title{Flag unusual points in a series}
\usage{
detect_anomalies(
  df,
  date_agg = "day",
  method = c("iqr", "zscore"),
  threshold = 1.5
)
}
\arguments{
\item{df}{A tibble with \code{ds}/\code{y} columns.}

\item{date_agg}{Aggregation frequency: one of \code{"hour"}, \code{"day"},
\code{"week"}, \code{"month"}, \code{"quarter"}, \code{"year"}.}

\item{method}{\code{"iqr"} (the default) flags points outside
\code{threshold * IQR} from the quartiles; \code{"zscore"} flags points more
than \code{threshold} standard deviations from the mean.}

\item{threshold}{Numeric sensitivity threshold. Default \code{1.5}.}
}
\value{
A tibble with columns \code{ds}, \code{y}, \code{score} (the value the
threshold was applied to), and \code{is_anomaly} (logical).
}
\description{
A statistical check on the values themselves -- distinct from the
Holidays tab's consistency check (calendar-vs-declared-holiday
mismatch). Uses the STL remainder when \code{\link[=decompose_series]{decompose_series()}} finds one
(isolates unusual points from normal trend/seasonal movement, which a
raw-value check can't); falls back to deviation from a running median
when no seasonal decomposition applies (short series, yearly
aggregation, etc.), so it still works on every series shape.
}
\examples{
df <- tibble::tibble(ds = as.Date("2024-01-01") + 0:29, y = c(rep(10, 29), 100))
detect_anomalies(df, date_agg = "day", method = "iqr")
}
