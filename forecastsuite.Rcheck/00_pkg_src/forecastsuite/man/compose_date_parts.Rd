% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/date_parts.R
\name{compose_date_parts}
\alias{compose_date_parts}
\title{Assemble a date from separate Year/Quarter/Month/Day columns}
\usage{
compose_date_parts(
  df,
  year_col,
  quarter_col = NULL,
  month_col = NULL,
  day_col = NULL
)
}
\arguments{
\item{df}{A data frame containing the part columns.}

\item{year_col}{Character scalar, the Year column's name. Required.}

\item{quarter_col}{Optional character scalar, a Quarter column (accepts
\code{1}-\code{4}, \code{"Q1"}-\code{"Q4"}, \code{"Quarter 2"}, etc.). Supply at most one of
\code{quarter_col}/\code{month_col}.}

\item{month_col}{Optional character scalar, a Month column (accepts
\code{1}-\code{12}, \code{"Jan"}, \code{"January"}, etc.).}

\item{day_col}{Optional character scalar, a Day-of-month column. Only
meaningful alongside \code{month_col}.}
}
\value{
A list with \code{ds} (a \code{Date} vector) and \code{granularity} (one of
\code{"year"}, \code{"quarter"}, \code{"month"}, \code{"day"}, the finest part supplied).
}
\description{
Real-world tables often split the period across columns (a Year column
plus a Quarter or Month column, sometimes plus a Day column) rather than
carrying one parseable date. This assembles those into a single \code{Date}
and reports the finest granularity present, so the caller can aggregate
at that level (Year+Quarter -> quarterly, Year+Month -> monthly, and so
on) instead of guessing.
}
\examples{
df <- data.frame(Year = c(2023, 2023, 2024), Month = c("Jan", "Feb", "Jan"))
compose_date_parts(df, year_col = "Year", month_col = "Month")
}
