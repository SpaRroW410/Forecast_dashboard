% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rolling_cv.R
\name{run_backtest_leaderboard}
\alias{run_backtest_leaderboard}
\title{Multi-model rolling-origin backtest leaderboard}
\usage{
run_backtest_leaderboard(
  model_keys,
  df,
  date_agg,
  horizon_periods,
  k_requested,
  build_args = function(model_key, train_df) {
     list(train_df = train_df, date_agg =
    date_agg)
 },
  window = c("expanding", "rolling"),
  train_window = NULL
)
}
\arguments{
\item{model_keys}{Character vector of registry keys (see
\code{\link[=list_models]{list_models()}}) to include in the leaderboard.}

\item{df}{A data frame with \code{ds}/\code{y} columns -- the series to backtest.}

\item{date_agg}{Aggregation frequency (e.g. \code{"day"}, \code{"month"}), passed
through to each model's \code{fit()}.}

\item{horizon_periods}{Integer, each fold's test window length (see
\code{\link[=build_cv_folds]{build_cv_folds()}}).}

\item{k_requested}{Integer, the number of folds requested (may return
fewer; see \code{\link[=build_cv_folds]{build_cv_folds()}}).}

\item{build_args}{A function \verb{function(model_key, train_df)} returning
the full argument list for that model's \code{fit()} call (\code{train_df}/
\code{date_agg} included). The default passes \code{train_df}/\code{date_agg} through
with no model-specific tuning; callers wanting each model's
currently-configured parameters (e.g. the bundled app, which passes
its own \code{build_fit_args()}) can supply their own.}

\item{window}{\code{"expanding"} (the default) or \code{"rolling"}, passed through
to \code{\link[=build_cv_folds]{build_cv_folds()}} -- see there for what each means.}

\item{train_window}{Integer or \code{NULL}, passed through to
\code{\link[=build_cv_folds]{build_cv_folds()}} when \code{window = "rolling"}.}
}
\value{
A tibble with one row per model in \code{model_keys}, columns \code{model}
(label), \code{key}, \code{n_folds}, \code{mean_mase}, \code{sd_mase}, \code{mean_smape},
\code{mean_rmse}, ranked ascending by \code{mean_mase} (\code{NA} last).
}
\description{
Runs \code{\link[=build_cv_folds]{build_cv_folds()}} once and refits every model in \code{model_keys}
across every fold, ranking them by mean test MASE -- a real multi-model
extension of single-model cross-validation. A fold a model fails on is
dropped for that model rather than failing the whole row; a model with
zero successful folds (including an invalid \code{model_keys} entry) gets an
\code{NA} row instead of being dropped, so it's still visible in the result as
"failed."
}
\examples{
df <- data.frame(ds = as.Date("2024-01-01") + 0:59,
                  y = 100 + seq_len(60) * 0.2 + stats::rnorm(60, sd = 2))
\donttest{
run_backtest_leaderboard(c("arima", "ets"), df, date_agg = "day",
                          horizon_periods = 5, k_requested = 2)
}
}
