% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recommend.R
\name{recommend_model}
\alias{recommend_model}
\title{Rank registered models for a series}
\usage{
recommend_model(analysis, holidays_configured = FALSE, candidates = NULL)
}
\arguments{
\item{analysis}{The list returned by \code{\link[=analyze_series]{analyze_series()}}.}

\item{holidays_configured}{Logical; whether holidays are configured for
this dataset. Every model except Prophet is scored down when \code{TRUE},
since none of the others model holiday effects.}

\item{candidates}{Character vector of registry keys to score (see
\code{\link[=list_models]{list_models()}}), or \code{NULL} (the default) to score every currently
available registered model.}
}
\value{
A data frame with columns \code{model} (label), \code{key}, \code{score}, and
\code{reason}, sorted by \code{score} descending.
}
\description{
Scores every candidate model against \code{\link[=analyze_series]{analyze_series()}}'s measured
trend/seasonality/regularity, with short human-readable reasons -- no
model is actually fit, so this is fast enough to run right after a
dataset is finalized.
}
\examples{
df <- tibble::tibble(ds = as.Date("2024-01-01") + 0:59, y = 1:60 + rnorm(60))
recommend_model(analyze_series(df, "day"))
}
