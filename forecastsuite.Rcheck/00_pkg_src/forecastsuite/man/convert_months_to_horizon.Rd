% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/horizon_utils.R
\name{convert_months_to_horizon}
\alias{convert_months_to_horizon}
\title{Convert a forecast horizon in months to periods}
\usage{
convert_months_to_horizon(months, aggregation)
}
\arguments{
\item{months}{Numeric, the horizon in months (as set by the app's
"Forecast Horizon (months)" slider).}

\item{aggregation}{One of \code{"hour"}, \code{"day"}, \code{"week"}, \code{"month"},
\code{"quarter"}, \code{"year"} -- the series' aggregation frequency.}
}
\value{
A numeric horizon in the same units as \code{aggregation} (e.g. days,
for \code{aggregation = "day"}), suitable for a model's \code{forecast(model, h)}
call.
}
\description{
Convert a forecast horizon in months to periods
}
\examples{
convert_months_to_horizon(12, "day")
convert_months_to_horizon(12, "month")
}
