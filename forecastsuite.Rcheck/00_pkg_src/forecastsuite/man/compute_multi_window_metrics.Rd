% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_window_metrics.R
\name{compute_multi_window_metrics}
\alias{compute_multi_window_metrics}
\title{Score a forecast over several evaluation windows at once}
\usage{
compute_multi_window_metrics(forecast_df, train_df, test_df, date_agg = "day")
}
\arguments{
\item{forecast_df}{A tibble with \code{ds}/\code{yhat} columns.}

\item{train_df}{A tibble with \code{ds}/\code{y} columns -- the training data.}

\item{test_df}{A tibble with \code{ds}/\code{y} columns -- the held-out test data.}

\item{date_agg}{Aggregation frequency (currently unused directly, kept
for interface consistency with the rest of the package).}
}
\value{
A tibble with columns \code{Set}, \code{Metric}, \code{Value} -- one block per
window that applied.
}
\description{
A single aggregate score hides where a model actually fails -- it can
look strong on the full training range while drifting badly over the
most recent months. Scores Train / Last 6 Months / Last 2 Years / Test
(held out) using \code{\link[=safe_compute_metrics]{safe_compute_metrics()}}, skipping windows the series
is too short to contain rather than reporting them as \code{NA}.
}
\examples{
ds <- as.Date("2024-01-01") + 0:29
train_df <- tibble::tibble(ds = ds[1:20], y = 1:20)
test_df <- tibble::tibble(ds = ds[21:30], y = 21:30)
forecast_df <- tibble::tibble(ds = ds, yhat = 1:30 + 0.5)
compute_multi_window_metrics(forecast_df, train_df, test_df)
}
