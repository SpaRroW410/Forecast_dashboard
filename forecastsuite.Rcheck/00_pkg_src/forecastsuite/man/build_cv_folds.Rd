% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rolling_cv.R
\name{build_cv_folds}
\alias{build_cv_folds}
\title{Build rolling-origin (walk-forward) cross-validation folds}
\usage{
build_cv_folds(
  df,
  horizon_periods,
  k_requested,
  min_train = 2,
  window = c("expanding", "rolling"),
  train_window = NULL
)
}
\arguments{
\item{df}{A data frame with a \code{ds} column, in any row order (it's sorted
internally).}

\item{horizon_periods}{Integer, the length of each fold's test window,
in the same units as \code{df$ds}'s spacing.}

\item{k_requested}{Integer, the number of folds requested (may return
fewer).}

\item{min_train}{Integer, the minimum number of training rows a fold
must have to be included. Default \code{2}.}

\item{window}{\code{"expanding"} (the default -- training windows always
start from the beginning of the series, growing fold over fold) or
\code{"rolling"} (each fold's training window is capped to the most recent
\code{train_window} rows instead, a fixed width -- useful for testing
robustness to concept drift/regime change separately from an
ever-larger training set).}

\item{train_window}{Integer, the fixed training-window width used when
\code{window = "rolling"}. Ignored for \code{"expanding"}. Defaults to \code{NULL},
which uses what would be the smallest (earliest) fold's expanding
training size -- a sensible width with no extra input needed.}
}
\value{
A list of folds, oldest-first (fold 1 has the smallest training
window under \code{"expanding"}, or the same fixed width under
\code{"rolling"}), each a list with \code{train} and \code{test} data frames.
}
\description{
Builds up to \code{k_requested} chronological, non-overlapping walk-forward
folds from one \code{ds}-ordered series. Each fold's test window is exactly
\code{horizon_periods} rows long. Degrades gracefully: if the series can't
support \code{k_requested} folds, fewer are returned instead of erroring --
possibly zero, for a very short series.
}
\examples{
df <- data.frame(ds = as.Date("2024-01-01") + 0:29, y = 1:30)
folds <- build_cv_folds(df, horizon_periods = 5, k_requested = 3)
length(folds)
rolling_folds <- build_cv_folds(df, horizon_periods = 5, k_requested = 3,
                                 window = "rolling", train_window = 10)
length(rolling_folds)
}
