% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_utils.R
\name{safe_compute_metrics}
\alias{safe_compute_metrics}
\title{Compute forecast metrics, guarding against a too-short test set}
\usage{
safe_compute_metrics(forecast_df, actual_df, label)
}
\arguments{
\item{forecast_df}{A tibble with \code{ds}/\code{yhat} columns.}

\item{actual_df}{A tibble with \code{ds}/\code{y} columns.}

\item{label}{Character scalar identifying this evaluation.}
}
\value{
A tibble with columns \code{Set}, \code{Metric}, \code{Value} (see
\code{\link[=compute_forecast_metrics]{compute_forecast_metrics()}}).
}
\description{
Same as \code{\link[=compute_forecast_metrics]{compute_forecast_metrics()}}, but checks \code{actual_df}'s row count
first so callers don't need their own length guard before calling it.
}
\examples{
ds <- as.Date("2024-01-01") + 0:9
forecast_df <- tibble::tibble(ds = ds, yhat = 1:10 + 0.5)
actual_df <- tibble::tibble(ds = ds, y = 1:10)
safe_compute_metrics(forecast_df, actual_df, label = "Test")
}
