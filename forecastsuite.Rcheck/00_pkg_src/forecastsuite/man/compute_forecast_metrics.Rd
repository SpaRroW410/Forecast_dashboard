% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metric_utils.R
\name{compute_forecast_metrics}
\alias{compute_forecast_metrics}
\title{Compute MASE, sMAPE, and RMSE for a forecast}
\usage{
compute_forecast_metrics(actual_df, forecast_df, label = "Test")
}
\arguments{
\item{actual_df}{A tibble with \code{ds}/\code{y} columns -- the held-out actuals.}

\item{forecast_df}{A tibble with \code{ds}/\code{yhat} columns -- the forecast to
evaluate.}

\item{label}{Character scalar identifying this evaluation (shown as the
\code{Set} column), e.g. \code{"Test"}.}
}
\value{
A tibble with columns \code{Set}, \code{Metric} (\code{"MASE"}, \code{"sMAPE (\%)"},
\code{"RMSE"}), and \code{Value} -- \code{NA} for all three if fewer than 2 rows
overlap.
}
\description{
Compute MASE, sMAPE, and RMSE for a forecast
}
\examples{
ds <- as.Date("2024-01-01") + 0:9
actual_df <- tibble::tibble(ds = ds, y = 1:10)
forecast_df <- tibble::tibble(ds = ds, yhat = 1:10 + 0.5)
compute_forecast_metrics(actual_df, forecast_df, label = "Test")
}
