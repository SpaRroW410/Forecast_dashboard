% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest_params.R
\name{suggest_parameters_for}
\alias{suggest_parameters_for}
\title{Suggest starting hyperparameters for several models at once}
\usage{
suggest_parameters_for(analysis, model_keys)
}
\arguments{
\item{analysis}{The list returned by \code{\link[=analyze_series]{analyze_series()}}.}

\item{model_keys}{Character vector of registry keys.}
}
\value{
A character vector, the same length as \code{model_keys}, of each
model's suggestion text.
}
\description{
Vectorized \code{\link[=suggest_parameters]{suggest_parameters()}}, for the recommendation table's
"suggested settings" column.
}
\examples{
df <- tibble::tibble(ds = as.Date("2024-01-01") + 0:59, y = 1:60 + rnorm(60))
suggest_parameters_for(analyze_series(df, "day"), c("arima", "prophet"))
}
