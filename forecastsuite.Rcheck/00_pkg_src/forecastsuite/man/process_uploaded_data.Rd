% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_utils.R
\name{process_uploaded_data}
\alias{process_uploaded_data}
\title{Turn an uploaded table into a ds/y forecasting series}
\usage{
process_uploaded_data(
  data,
  type = c("agg", "individual"),
  date_col,
  value_col = NULL,
  group_col = NULL,
  pop_df = NULL,
  pop_date_col = NULL,
  pop_value_col = NULL,
  pop_group_col = NULL,
  unit_divisor = 1,
  pop_multiplier = 1,
  date_agg = "day",
  pop_freq = "year"
)
}
\arguments{
\item{data}{A data frame -- the uploaded main dataset.}

\item{type}{\code{"agg"} (each row is already one value per period) or
\code{"individual"} (each row is a single event, counted per period).}

\item{date_col}{Character scalar, the name of \code{data}'s date column.}

\item{value_col}{Character scalar, the name of \code{data}'s value column
(required when \code{type = "agg"}, ignored for \code{"individual"}).}

\item{group_col}{Optional character scalar, a grouping column (e.g.
District) to carry through untouched alongside \code{ds}/\code{y}.}

\item{pop_df}{Optional population data frame for normalization.}

\item{pop_date_col, pop_value_col}{Character scalars naming \code{pop_df}'s key
and value columns (required together with \code{pop_df} to normalize).}

\item{pop_group_col}{Optional character scalar, \code{pop_df}'s grouping
column, if it has one.}

\item{unit_divisor, pop_multiplier}{Numeric scaling applied to the
population figure (e.g. \code{unit_divisor = 100000} for per-100k).}

\item{date_agg}{Aggregation frequency: one of \code{"hour"}, \code{"day"},
\code{"week"}, \code{"month"}, \code{"quarter"}, \code{"year"}.}

\item{pop_freq}{Whether \code{pop_df}'s value is given \code{"year"}ly or
\code{"month"}ly.}
}
\value{
A tibble with columns \code{ds}, \code{y}, and \code{group_col} if supplied.
}
\description{
The Import tab's core transform: renames the user's chosen date/value
columns to \code{ds}/\code{y}, aggregates individual-observation data into periods,
and optionally normalizes by a population table to produce incidence
rather than absolute counts.
}
\examples{
df <- data.frame(when = as.Date("2024-01-01") + 0:9, count = 1:10)
process_uploaded_data(df, type = "agg", date_col = "when", value_col = "count")
}
