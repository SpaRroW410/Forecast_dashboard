% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{compute_residual_diagnostics}
\alias{compute_residual_diagnostics}
\title{Residual diagnostics for a forecast}
\usage{
compute_residual_diagnostics(fc_tib, test_df)
}
\arguments{
\item{fc_tib}{A tibble with \code{ds}/\code{yhat} columns -- a model's forecast.}

\item{test_df}{A tibble with \code{ds}/\code{y} columns -- the held-out actuals.}
}
\value{
A list with \code{residuals} (tibble of \code{ds}/\code{resid}), \code{ljung_box_p},
\code{shapiro_p} (each \code{NA} if there isn't enough overlapping data),
\code{acf_df} (tibble of \code{lag}/\code{acf}), \code{ci} (the confidence bound), and \code{n}.
}
\description{
Whether a fitted model's error looks like unpredictable noise, or still
has structure left in it: a Ljung-Box test for residual autocorrelation,
a Shapiro-Wilk normality test, and the residual ACF with its Box-Jenkins
confidence band. Generic across every registered model -- only needs
\code{fc_tib}'s \code{ds}/\code{yhat} and \code{test_df}'s \code{ds}/\code{y}.
}
\examples{
ds <- as.Date("2024-01-01") + 0:29
fc_tib <- tibble::tibble(ds = ds, yhat = 100)
test_df <- tibble::tibble(ds = ds, y = 100 + rnorm(30))
compute_residual_diagnostics(fc_tib, test_df)
}
