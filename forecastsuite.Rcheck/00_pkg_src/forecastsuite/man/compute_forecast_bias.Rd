% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bias_drift.R
\name{compute_forecast_bias}
\alias{compute_forecast_bias}
\title{Signed forecast error (bias)}
\usage{
compute_forecast_bias(fc_tib, test_df)
}
\arguments{
\item{fc_tib}{A tibble with \code{ds}/\code{yhat} columns -- a model's forecast.}

\item{test_df}{A tibble with \code{ds}/\code{y} columns -- the held-out actuals.}
}
\value{
A list with \code{bias} (mean signed error, \code{y - yhat}), \code{pct_bias}
(\code{bias} as a percentage of mean \verb{|y|}), and \code{n}. All \code{NA}/\code{0} if there's
no overlap.
}
\description{
MASE/sMAPE/RMSE are all magnitude-only, so a model that's consistently
over- or under-forecasting can still look "fine" on those alone. This
surfaces the signed error directly. Generic across every registered
model -- only needs \code{fc_tib}'s \code{ds}/\code{yhat} and \code{test_df}'s \code{ds}/\code{y}.
}
\examples{
ds <- as.Date("2024-01-01") + 0:4
fc_tib <- tibble::tibble(ds = ds, yhat = rep(8, 5))
test_df <- tibble::tibble(ds = ds, y = rep(10, 5))
compute_forecast_bias(fc_tib, test_df)
}
