% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_generic.R
\name{plot_model_comparison}
\alias{plot_model_comparison}
\title{Overlay several models' forecasts on one plot}
\usage{
plot_model_comparison(forecasts, train_df = NULL, title = "Model Comparison")
}
\arguments{
\item{forecasts}{A named list of \code{ds}/\code{yhat} tibbles; names are used as
the legend labels.}

\item{train_df}{Optional tibble with \code{ds}/\code{y} columns, drawn as the
"Actual" line.}

\item{title}{Character scalar, the plot title. Default
\code{"Model Comparison"}.}
}
\value{
A \code{plotly} htmlwidget.
}
\description{
Used for the "Compare Selected Models" flow (and forecast ensembling,
which adds one more named entry).
}
\examples{
ds <- as.Date("2024-01-01") + 0:9
forecasts <- list(
  ARIMA = tibble::tibble(ds = ds, yhat = 1:10),
  ETS = tibble::tibble(ds = ds, yhat = 1:10 + 1)
)
plot_model_comparison(forecasts)
}
