% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contingency.R
\name{always_zero_dates}
\alias{always_zero_dates}
\title{Find calendar dates that are always zero or missing}
\usage{
always_zero_dates(data, holidays = NULL)
}
\arguments{
\item{data}{A tibble with \code{ds}/\code{y} columns, the finalized dataset.}

\item{holidays}{Optional tibble with a \code{ds} column, the compiled holiday
list -- used only to flag which suspicious dates are already declared.}
}
\value{
A tibble with columns \code{ds}, \code{flag} (\code{"Zero every year"} or
\code{"Missing every year"}), and \code{declared_holiday} (logical) -- the
undeclared rows are the actual gap.
}
\description{
Calendar dates (month-day) that are zero or missing in every year
present -- these look like de facto holidays/closures that were never
declared, so a holiday-aware model can't account for them.
}
\examples{
data <- tibble::tibble(
  ds = c(as.Date("2023-01-01"), as.Date("2024-01-01"), as.Date("2023-06-01")),
  y = c(0, 0, 5)
)
always_zero_dates(data)
}
