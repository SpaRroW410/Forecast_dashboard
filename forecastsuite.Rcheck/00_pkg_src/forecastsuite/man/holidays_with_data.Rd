% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contingency.R
\name{holidays_with_data}
\alias{holidays_with_data}
\title{Find declared holidays with non-zero data}
\usage{
holidays_with_data(holidays, data)
}
\arguments{
\item{holidays}{A tibble with a \code{ds} column (and typically \code{holiday}),
the compiled holiday list.}

\item{data}{A tibble with \code{ds}/\code{y} columns, the finalized dataset.}
}
\value{
A tibble with columns \code{ds}, \code{holiday}, \code{y}, sorted by \code{y}
descending; empty if \code{holidays}/\code{data} are \code{NULL}/empty.
}
\description{
Days declared as holidays that nonetheless carry non-zero values --
usually means the holiday wasn't actually observed at that site, or data
entry continued anyway.
}
\examples{
holidays <- tibble::tibble(ds = as.Date("2024-01-01"), holiday = "New Year")
data <- tibble::tibble(ds = as.Date("2024-01-01"), y = 5)
holidays_with_data(holidays, data)
}
